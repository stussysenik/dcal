/* Domain Layer — Pure logic, zero I/O, 100% testable.
   Value types and protocols that define the core calibration domain.
   No Foundation imports beyond what Swift provides natively. */

// MARK: - Placeholder

/// Namespace marker for the Domain module.
/// Real types (ColorMath, GammaModel, etc.) will replace this.
public enum DomainModule {
    public static let version = "0.1.0"
}
